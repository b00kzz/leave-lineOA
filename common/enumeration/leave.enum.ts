export enum LEAVE_STATUS {
    Pending = `pending approval`,
    Approved = `approved`,
    Rejected = `rejected`,
    Cancelled = `cancelled`,
}

export enum LEAVE_DAY_TYPE {
    Morning = `morning`,
    Afternoon = `afternoon`,
    FullDay = `full day`,
}
